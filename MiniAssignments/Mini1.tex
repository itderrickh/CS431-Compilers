\documentclass[12pt]{article}
\usepackage{tikz}
\usetikzlibrary{trees}

%% Language and font encodings
\usepackage{amsmath}
\usepackage[english]{babel}
\usepackage[utf8x]{inputenc}
\usepackage[T1]{fontenc}

\setlength{\oddsidemargin}{0pt}
\setlength{\evensidemargin}{0pt}
\setlength{\textwidth}{6.5in}
\setlength{\topmargin}{0in}
\setlength{\textheight}{8.5in}

\newcommand{\assignNum}{1}
\newcommand{\yourName}{Derrick Heinemann}

\begin{document}

% Fill these in!

%This title line doesn't do anything.  However, it allows your to rename your 
%document so you can distinguish it from others on the writelatex dashboard.
\title{Compilers Mini 1}
\noindent{\textit{Compilers: Computer Science 431}\newline
\yourName \newline
\mbox{}\hrulefill}
\begin{center}{\Large\bf Mini \assignNum}\end{center}


Given the following code: $z = a - b * a + b$, translate it into:

\begin{enumerate}
	\item A sequence of tokens that the scanner would output. \\
    $<$id, 1$>$ $<$=$>$ $<$id, 2$>$ $<$-$>$ $<$id, 3$>$ $<$*$>$ $<$id, 2$>$ $<$+$>$ $<$id, 3$>$
    \item Given the tokens from 1, a syntax tree such that there is normal precedence. \\
    
    \begin{tikzpicture}[level distance=1.5cm,
  level 1/.style={sibling distance=3cm},
  level 2/.style={sibling distance=3cm},
  level 3/.style={sibling distance=3cm}]
  \node {<=>}
    child {node {<id, 1>}}
    child {node {<+>}
    	child {node {<id, 3>}}
        child {node {<->}
        	child {node {<id, 2>}}
            child {node {<*>}
            	child {node {<id, 2>}}
                child {node {<id, 3>}}}}
        };
\end{tikzpicture}
    
    
  %  \item Given the tree from 2, three-address code where there is no optimization.
  %  \item Given the code from 3, reasonable assembly code.
\end{enumerate}


\end{document}